[pytest]
# asyncio_mode=auto lets plain `async def test_*` run without per-test markers,
# and we also keep explicit @pytest.mark.asyncio for clarity in a few places.
asyncio_mode = auto
testpaths = tests
